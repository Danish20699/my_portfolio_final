/**
 * Real client testimonials only.
 *
 * The section renders nothing while this array is empty, so the page stays
 * honest until you have quotes you can actually attribute to a real person.
 *
 * Shape:
 * {
 *   name:    'Full Name',
 *   role:    'Title at Company',
 *   text:    'What they actually said.',
 *   image:   '/images/testimonials/their-photo.jpg', // optional — omit for initials
 *   source:  'https://linkedin.com/in/their-profile', // optional — links the name
 * }
 */
export const testimonials = [];
